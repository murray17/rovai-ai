# Rovai AI Third-Party Notices and Source Record

This file is the entry point for source and packaged-desktop legal materials.
Machine-readable exact-version lists and SHA-256 values live under `legal/`.

## 1. Rovai AI License

Rovai AI source code is distributed under the MIT License in the repository
root `LICENSE`. Third-party components and first-party artwork retain the
terms and provenance described below; the project MIT License does not replace
those terms.

## 2. AI-Generated Project Artwork

The listed illustrations, portraits, world map, and application-icon design
were generated by the Rovai AI project owner using OpenAI / ChatGPT
image-generation tools for this project. The owner approves redistribution in
the public source repository, packaged desktop applications, documentation,
and release materials. These are not third-party stock assets.

This statement does not grant rights in unrelated third-party trademarks,
characters, people, or source material. Exact tracked paths, embedded
prototype images, screenshot classifications, and hashes are recorded in
`legal/provenance/ai-generated-project-artwork.md` and
`legal/manifests/project-artwork.json`.

## 3. Bundled Skills

The application carries 13 official Skills. Each `skills/<name>/NOTICE`
records its source classification.

- `tasteful-ui`, `diagnosing-bugs`, `tdd`, and `writing-for-agents` retain
  their existing exact upstream revision, path, local-change, and MIT records.
- `grill-duo` and `grill-duo-with-docs` are Rovai-specific translated and
  adapted rewrites of the pinned `mattpocock/skills` sources identified in
  their NOTICE files. They retain the upstream MIT License.
- `campfire`, `review-duo`, `analyze-agent-codebase`, `cli-operations`,
  `member-studio`, `memory-stewardship`, and `worktree` have repository-history
  evidence of first-party development. No external vendored source was found
  in the audited history and source search except inspiration explicitly
  identified by a NOTICE. This is an evidence statement, not absolute proof
  that no earlier influence could ever be identified.

## 4. Runtime Logos

The 11 runtime SVGs are sourced from
`@lobehub/icons-static-svg 1.94.0`, MIT. Nine are byte-identical and two differ
only by a trailing newline; there are no material SVG changes. The exact table
and MIT text are in
`apps/desktop/src/renderer/src/assets/runtime-logos/ASSET-NOTICE.md` and
`legal/licenses/runtime-logos/LICENSE`.

Product names and logos remain trademarks or brand assets of their respective
rights holders. Inclusion identifies supported runtime products and does not
claim ownership of those marks.

## 5. Codex Schema

`schemas/codex-app-server/0.144.5` derives from OpenAI Codex tag
`rust-v0.144.5`, peeled commit
`87db9bc18ba5bc82c1cb4e4381b44f693ee35623`, under Apache-2.0. The local 267
files contain formatting-only JSON serialization differences and zero
semantic differences after normalized comparison. See
`legal/provenance/codex-schema.md` and `legal/licenses/codex/LICENSE`.

## 6. JavaScript Dependencies

The frozen pnpm graph contains 494 exact package instances: 149 production
instances and 345 development-only instances. Electron Builder packages 144
instances in `app.asar`; the five pruned production-graph-only instances are
recorded explicitly in `legal/manifests/javascript-binary-dependencies.json`.
The source graph, direct/transitive classification, SPDX expressions,
license-file source, and SHA-256 values are in
`legal/manifests/javascript-source-dependencies.json` and
`legal/licenses/javascript/`.

`react-remove-scroll-bar 2.3.8` declares MIT in its exact npm metadata and
README but omits a license file from the tarball. Its manifest entry records
the exact tarball SHA-256, published `gitHead`, and the later fixed upstream
commit that added the explicit MIT text; that text is included in the binary
notices. No unknown or custom license expression remains in the bundled set.

## 7. Rust Dependencies and SQLite

The macOS arm64 release graph contains Rovai Core plus 119 third-party crates.
Exact versions, direct/transitive classification, Cargo checksums,
repositories, SPDX expressions, and available package license files are in
`legal/manifests/rust-release-dependencies.json` and
`legal/licenses/rust/`.

`rusqlite 0.38.0` and `libsqlite3-sys 0.36.0` are MIT-licensed Rust crates.
The enabled `bundled` feature compiles SQLite 3.51.1. SQLite is public-domain
software and is not relicensed by Rovai AI under MIT; the packaged statement
quotes the amalgamation's dedication and links the canonical SQLite record.

`option-ext 0.2.0` is an unchanged transitive MPL-2.0 crate. Its exact archive,
checksum, source path, complete MPL text, and status are recorded in
`legal/provenance/option-ext-0.2.0.md`. Its compliance review remains
`FACTS_COLLECTED_REVIEW_PENDING`, so the public binary release gate is blocked.

## 8. Electron and Chromium

The legal payload preparation step reads Electron `43.1.1` from the frozen
installation, verifies the platform release archive against Electron's exact
`checksums.json` entry, and copies its root `LICENSE` and unmodified
`LICENSES.chromium.html` to `Contents/Resources/legal/electron/`. Both remain
directly readable outside `app.asar`; the generated Electron manifest records
their hashes without timestamps or machine-local paths.

## 9. Product Names and Trademarks

Rovai AI does not claim rights in third-party product names, logos,
trademarks, characters, or people merely because a compatibility surface,
notice, schema, or project artwork refers to them. All such rights remain with
their respective owners.
