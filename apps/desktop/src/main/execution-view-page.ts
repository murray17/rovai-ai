export const EXECUTION_VIEW_PAGE = String.raw`<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
  <meta name="color-scheme" content="light dark">
  <title>Rovai · 执行台</title>
  <style>
    :root{color-scheme:light;--canvas:#eceeef;--surface:#fbfbfa;--raised:#fff;--topbar:#fbfbfa;--ink:#171b20;--muted:#616a73;--faint:#6e7382;--line:#dfe4e8;--brand:#526f88;--brand-ink:#405f7e;--brand-soft:#e9eef3;--identity-user:#a65f4a;--identity-agent:#39777a;--success:#3e775c;--success-soft:#e7f1ea;--danger:#a24c46;--danger-soft:#f7e6e3;--attention:#8a6226;--attention-soft:#f8edda;--evidence-canvas:#f4f6f3;--evidence-surface:#fff;--evidence-ink:#252a36;--evidence-muted:#5f6678;--evidence-line:#d5dad3;--diff-add:#137333;--diff-remove:#b3261e;font-family:-apple-system,BlinkMacSystemFont,"Segoe UI","PingFang SC","Hiragino Sans GB","Microsoft YaHei",sans-serif}
    @media(prefers-color-scheme:dark){:root{color-scheme:dark;--canvas:#0d1114;--surface:#151a1e;--raised:#1b2227;--topbar:#151a1e;--ink:#e7ecef;--muted:#abb5bc;--faint:#919da6;--line:#333e46;--brand:#7897ae;--brand-ink:#c0d4e1;--brand-soft:#22303a;--identity-user:#c98572;--identity-agent:#70b0ae;--success:#82b695;--success-soft:#1a2b22;--danger:#d6857f;--danger-soft:#321e1d;--attention:#d2ac70;--attention-soft:#302719;--evidence-canvas:#12191d;--evidence-surface:#171f24;--evidence-ink:#dce4e9;--evidence-muted:#9eabb3;--evidence-line:#35424a;--diff-add:#92c7a5;--diff-remove:#e09a94}}
    *{box-sizing:border-box}html{min-height:100%;background:var(--canvas)}body{min-width:0;margin:0;color:var(--ink);background:var(--canvas);font-size:14px;line-height:1.6}.skip{position:fixed;z-index:10;top:8px;left:8px;padding:8px 10px;border-radius:7px;color:var(--surface);background:var(--ink);transform:translateY(-150%)}.skip:focus{transform:none}.topbar{position:sticky;z-index:5;top:0;border-bottom:1px solid color-mix(in srgb,var(--line) 82%,transparent);background:color-mix(in srgb,var(--topbar) 92%,transparent);backdrop-filter:blur(18px)}.topbar-inner{display:flex;width:min(980px,100%);min-height:54px;align-items:center;gap:11px;margin:auto;padding:0 22px}.brand{display:grid;width:25px;height:25px;place-items:center;color:var(--brand)}.brand svg{width:24px;height:24px;fill:none;stroke:currentColor;stroke-linecap:round;stroke-linejoin:round;stroke-width:1.45}.context{display:flex;min-width:0;flex:1;align-items:baseline;gap:7px}.context strong{overflow:hidden;font-size:13px;font-weight:660;text-overflow:ellipsis;white-space:nowrap}.context span{color:var(--faint);font-size:10.5px}.readonly{padding:2px 7px;border:1px solid var(--line);border-radius:999px;color:var(--muted);font-size:9.5px;font-weight:680;letter-spacing:.08em}.shell{width:min(980px,100%);margin:auto;padding:22px 22px 54px}.source{display:grid;grid-template-columns:34px minmax(0,1fr);gap:10px;padding:0 2px}.avatar,.agent-avatar{display:grid;place-items:center;border:1px solid color-mix(in srgb,currentColor 38%,var(--line));border-radius:50%;background:var(--raised);font-weight:700}.avatar{width:30px;height:30px;color:var(--identity-user);font-size:11px}.agent-avatar{color:var(--identity-agent)}.message-meta{display:flex;min-height:20px;flex-wrap:wrap;align-items:baseline;gap:7px}.message-meta strong{font-size:11px}.message-meta span,.message-meta time{color:var(--faint);font-size:9.5px}.message-copy{max-width:76ch;margin:2px 0 0;white-space:pre-wrap;overflow-wrap:anywhere;font-size:13px}.console{margin-top:22px;overflow:hidden;border:1px solid var(--line);border-radius:10px;background:var(--surface)}.console-head{display:flex;min-height:58px;align-items:center;gap:9px;padding:11px 18px;border-bottom:1px solid var(--line);background:var(--raised)}.agent-avatar{width:29px;height:29px;flex:0 0 auto;font-size:10px}.console-title{display:grid;min-width:0;flex:1;gap:1px}.console-title strong{overflow:hidden;font-size:12.5px;font-weight:680;text-overflow:ellipsis;white-space:nowrap}.console-title span{color:var(--faint);font-size:10.5px}.connection{display:flex;align-items:center;gap:6px;color:var(--muted);font-size:10.5px}.connection::before{width:6px;height:6px;border-radius:50%;background:var(--success);content:""}.connection.is-waiting::before{background:var(--attention)}.connection.is-offline::before{background:var(--danger)}.timeline{display:grid;gap:0;margin:0;padding:12px 18px 22px;list-style:none}.run{position:relative;display:grid;grid-template-columns:18px minmax(0,1fr);gap:9px}.run::before{position:absolute;top:18px;bottom:-2px;left:6px;width:1px;background:var(--line);content:""}.run:last-child::before{display:none}.run-node{position:relative;z-index:1;width:9px;height:9px;margin-top:18px;border:2px solid var(--surface);border-radius:50%;background:var(--faint);box-shadow:0 0 0 1px var(--line)}.run.is-focused .run-node,.run.is-running .run-node{background:var(--brand);box-shadow:0 0 0 1px color-mix(in srgb,var(--brand) 60%,var(--line))}.run-panel{min-width:0;padding:5px 0 20px}.run-select{display:flex;width:100%;min-height:46px;align-items:center;justify-content:space-between;gap:12px;padding:6px 9px;border:0;border-radius:7px;color:inherit;background:transparent;text-align:left;cursor:pointer;touch-action:manipulation}.run-select:hover{background:var(--raised)}.run-select:focus-visible{background:var(--raised);outline:2px solid var(--brand);outline-offset:1px}.run-title{display:grid;min-width:0;gap:2px}.run-title>span:first-child{display:flex;flex-wrap:wrap;align-items:center;gap:7px}.run-title time{font:600 10.5px/1.4 ui-monospace,SFMono-Regular,Menlo,monospace}.run-summary{overflow:hidden;color:var(--muted);font-size:10.5px;text-overflow:ellipsis;white-space:nowrap}.state{padding:1px 6px;border-radius:999px;color:var(--muted);background:var(--raised);font-size:9px;font-weight:670}.state.is-running{color:var(--brand-ink);background:var(--brand-soft)}.state.is-success{color:var(--success);background:var(--success-soft)}.state.is-danger{color:var(--danger);background:var(--danger-soft)}.state.is-attention{color:var(--attention);background:var(--attention-soft)}.current{color:var(--brand-ink);font-size:9px;font-weight:680}.run-body{display:grid;gap:9px;padding:3px 9px 0}.narration{max-width:76ch;margin:0;white-space:pre-wrap;overflow-wrap:anywhere;font-size:13px;line-height:1.65}.activity{overflow:hidden;border:1px solid var(--evidence-line);border-radius:8px;background:var(--evidence-canvas)}.activity-head{display:flex;min-height:37px;align-items:center;gap:8px;padding:7px 9px}.activity-icon{display:grid;width:19px;height:19px;place-items:center;border-radius:5px;color:var(--evidence-muted);background:var(--raised);font:700 10px/1 ui-monospace,SFMono-Regular,Menlo,monospace}.activity-title{min-width:0;flex:1;overflow-wrap:anywhere;color:var(--evidence-ink);font:550 11px/1.5 ui-monospace,SFMono-Regular,Menlo,monospace}.activity-status{flex:0 0 auto;color:var(--evidence-muted);font-size:9.5px}.result{margin:0;padding:9px;border-top:1px solid var(--evidence-line);color:var(--evidence-muted);background:var(--evidence-surface);font:10.5px/1.55 ui-monospace,SFMono-Regular,Menlo,monospace;white-space:pre-wrap;overflow-wrap:anywhere}.files{display:grid;gap:5px;padding:8px 9px;border-top:1px solid var(--evidence-line)}.file{display:flex;min-width:0;align-items:baseline;gap:8px;font-size:10.5px}.file code{min-width:0;flex:1;overflow-wrap:anywhere;color:var(--evidence-muted);font:inherit}.plus{color:var(--diff-add)}.minus{color:var(--diff-remove)}.empty{padding:28px 12px;color:var(--muted);text-align:center;font-size:11px}.failure{display:grid;place-items:center;min-height:58vh;padding:28px;text-align:center}.failure div{max-width:430px}.failure strong{display:block;margin-bottom:6px;font-size:15px}.failure p{margin:0;color:var(--muted);font-size:11px}
    @media(max-width:640px){.topbar-inner{min-height:50px;padding:0 12px}.context span{display:none}.shell{padding:17px 10px 42px}.source{grid-template-columns:31px minmax(0,1fr);padding:0 3px}.avatar{width:28px;height:28px}.message-copy{font-size:13px}.console{margin-top:18px;border-radius:8px}.console-head{min-height:55px;padding:10px 11px}.timeline{padding:8px 9px 17px}.run{grid-template-columns:15px minmax(0,1fr);gap:5px}.run::before{left:5px}.run-select{min-height:48px;padding:6px 7px}.run-panel{padding-bottom:14px}.run-body{padding:3px 7px 0}.activity-head{min-height:44px;align-items:flex-start}.activity-status{padding-top:2px}}
    @media(prefers-reduced-motion:reduce){*,*::before,*::after{scroll-behavior:auto!important;transition:none!important}}
  </style>
</head>
<body>
  <a class="skip" href="#console">跳到执行记录</a>
  <header class="topbar"><div class="topbar-inner"><span class="brand" aria-label="Rovai AI"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="M5.5 6.5 12 3l6.5 3.5v7L12 21l-6.5-7.5z"/><path d="m8.5 9 3.5 2 3.5-2M12 11v5"/></svg></span><div class="context"><strong id="camp-title">执行台</strong><span>局域网视图</span></div><span class="readonly">只读</span></div></header>
  <main class="shell" id="app"><div class="failure"><div><strong>正在读取执行记录</strong><p>请保持 Rovai 在本机运行。</p></div></div></main>
  <script>
  (()=>{
    'use strict';
    const app=document.getElementById('app');
    const campTitle=document.getElementById('camp-title');
    const match=location.pathname.match(/^\/execution\/([A-Za-z0-9_-]{1,200})$/);
    const runId=match?match[1]:'';
    const params=new URLSearchParams(location.hash.slice(1));
    const token=params.get('t')||'';
    history.replaceState(null,'',location.pathname);
    let snapshot=null;
    let selectedRunId=runId;
    let stopped=false;
    const auth={'Authorization':'Bearer '+token,'Accept':'application/json'};
    const text=(tag,value,className)=>{const node=document.createElement(tag);if(className)node.className=className;node.textContent=value||'';return node};
    const fail=(title,detail)=>{stopped=true;app.replaceChildren();const shell=text('div','', 'failure');const box=document.createElement('div');box.append(text('strong',title),text('p',detail));shell.append(box);app.append(shell)};
    const status=(run)=>{const map={queued:['已排队',''],running:['执行中','is-running'],waiting:['等待处理','is-attention'],succeeded:['已完成','is-success'],failed:['执行失败','is-danger'],cancelled:['已停止','']};return map[run.status]||[run.status,'']};
    const clock=(value)=>{const d=new Date(value);return Number.isNaN(d.getTime())?'':new Intl.DateTimeFormat('zh-CN',{hour:'2-digit',minute:'2-digit'}).format(d)};
    const timeRange=(run)=>clock(run.startedAt||run.createdAt)+'–'+(run.endedAt?clock(run.endedAt):'现在');
    const setSource=(run)=>{selectedRunId=run.id;document.querySelectorAll('.run').forEach(n=>{const selected=n.dataset.runId===run.id;n.classList.toggle('is-focused',selected);n.querySelector('.run-select')?.setAttribute('aria-pressed',String(selected))});const avatar=document.getElementById('source-avatar');const meta=document.getElementById('source-meta');const copy=document.getElementById('source-copy');if(avatar)avatar.textContent=(run.trigger.authorDisplayName||'消').slice(0,1);if(meta){meta.replaceChildren(text('strong',run.trigger.authorDisplayName||'触发消息'),text('span',run.trigger.channelLabel||'飞书'),text('time',clock(run.trigger.createdAt||run.createdAt)))}if(copy)copy.textContent=run.trigger.summary||'这次执行没有可显示的触发消息摘要。'};
    const activity=(item)=>{const box=document.createElement('section');box.className='activity';const head=document.createElement('div');head.className='activity-head';head.append(text('span',item.icon||'>','activity-icon'),text('strong',item.title||'执行记录','activity-title'),text('span',item.statusLabel||'','activity-status'));box.append(head);if(item.result)box.append(text('pre',item.result,'result'));if(Array.isArray(item.files)&&item.files.length){const files=document.createElement('div');files.className='files';item.files.forEach(f=>{const row=document.createElement('div');row.className='file';row.append(text('code',f.path));if(Number.isFinite(f.additions))row.append(text('span','+'+f.additions,'plus'));if(Number.isFinite(f.deletions))row.append(text('span','−'+f.deletions,'minus'));files.append(row)});box.append(files)}return box};
    const render=()=>{if(!snapshot)return;campTitle.textContent=snapshot.camp.title||'执行台';app.replaceChildren();const source=document.createElement('section');source.className='source';source.setAttribute('aria-label','触发消息');source.setAttribute('aria-live','polite');source.append(text('span','消','avatar'));source.firstChild.id='source-avatar';const message=document.createElement('div');const meta=document.createElement('div');meta.className='message-meta';meta.id='source-meta';const copy=text('p','','message-copy');copy.id='source-copy';message.append(meta,copy);source.append(message);app.append(source);const consoleBox=document.createElement('section');consoleBox.className='console';consoleBox.id='console';const head=document.createElement('header');head.className='console-head';head.append(text('span',(snapshot.agent.displayName||'队').slice(0,1),'agent-avatar'));const title=document.createElement('div');title.className='console-title';title.append(text('strong',(snapshot.agent.displayName||'队员')+' · 执行记录'),text('span',snapshot.runs.length+' 次运行'));head.append(title);const connection=text('span',snapshot.terminal?'执行已结束':'实时更新','connection'+(snapshot.terminal?'':' is-waiting'));connection.setAttribute('role','status');head.append(connection);consoleBox.append(head);const list=document.createElement('ol');list.className='timeline';if(!snapshot.runs.length)list.append(text('li','没有可显示的执行记录。','empty'));snapshot.runs.forEach(run=>{const li=document.createElement('li');li.className='run'+(run.status==='running'?' is-running':'')+(run.id===selectedRunId?' is-focused':'');li.dataset.runId=run.id;li.append(text('span','','run-node'));const panel=document.createElement('article');panel.className='run-panel';const button=document.createElement('button');button.type='button';button.className='run-select';button.setAttribute('aria-label','查看 '+timeRange(run)+' 的触发消息');button.setAttribute('aria-pressed',String(run.id===selectedRunId));const rt=document.createElement('span');rt.className='run-title';const top=document.createElement('span');top.append(text('time',timeRange(run)));const s=status(run);top.append(text('span',s[0],'state '+s[1]));if(run.id===snapshot.focusRunId)top.append(text('span','当前','current'));rt.append(top,text('span',run.purpose||run.trigger.summary||'执行','run-summary'));button.append(rt);button.addEventListener('click',()=>setSource(run));panel.append(button);const body=document.createElement('div');body.className='run-body';(run.items||[]).forEach(item=>{if(item.kind==='narration')body.append(text('p',item.body,'narration'));else body.append(activity(item))});if(!(run.items||[]).length)body.append(text('p','暂无公开执行记录。','narration'));panel.append(body);li.append(panel);list.append(li)});consoleBox.append(list);app.append(consoleBox);const selected=snapshot.runs.find(r=>r.id===selectedRunId)||snapshot.runs.find(r=>r.id===snapshot.focusRunId)||snapshot.runs[0];if(selected)setSource(selected)};
    const acceptSnapshot=(value)=>{if(!value||value.schemaVersion!==1||value.focusRunId!==runId||!Array.isArray(value.runs))throw new Error('invalid_projection');snapshot=value;if(!snapshot.runs.some(r=>r.id===selectedRunId))selectedRunId=runId;render()};
    const readSnapshot=async()=>{const response=await fetch('/api/execution/'+encodeURIComponent(runId)+'/snapshot',{headers:auth,cache:'no-store'});if(response.status===401||response.status===410)throw new Error('invalid_token');if(!response.ok)throw new Error('unavailable');acceptSnapshot(await response.json())};
    const stream=async()=>{if(stopped||snapshot?.terminal)return;const response=await fetch('/api/execution/'+encodeURIComponent(runId)+'/events',{headers:{...auth,'Accept':'text/event-stream'},cache:'no-store'});if(response.status===401||response.status===410)throw new Error('invalid_token');if(!response.ok||!response.body)throw new Error('unavailable');const reader=response.body.getReader();const decoder=new TextDecoder();let buffer='';while(!stopped){const part=await reader.read();if(part.done)throw new Error('disconnected');buffer+=decoder.decode(part.value,{stream:true});let cut;while((cut=buffer.indexOf('\n\n'))>=0){const frame=buffer.slice(0,cut);buffer=buffer.slice(cut+2);const data=frame.split('\n').filter(l=>l.startsWith('data:')).map(l=>l.slice(5).trim()).join('');if(!data)continue;const message=JSON.parse(data);if(message.type==='snapshot')acceptSnapshot(message.snapshot);if(message.type==='terminal'){acceptSnapshot(message.snapshot);stopped=true;return}if(message.type==='invalidated')throw new Error('invalid_token')}}};
    const follow=async()=>{while(!stopped&&!snapshot?.terminal){try{await stream()}catch(error){if(error.message==='invalid_token')throw error;if(!stopped)await new Promise(resolve=>setTimeout(resolve,1200))}}};
    if(!runId||!token){fail('此执行台链接已失效','请回到飞书执行卡重新打开。');return}
    readSnapshot().then(()=>follow()).catch(error=>{if(error.message==='invalid_token')fail('此执行台链接已失效','请回到飞书执行卡重新打开。');else fail('暂时无法读取执行记录','确认手机或电脑与 Rovai 位于同一局域网后重试。')});
  })();
  </script>
</body>
</html>`
