---
document_type: implementation-plan
version: v1.57
status: in-progress
last_updated: 2026-09-10
---

# 评测证据与报告完整性修订

## 问题、复用与最小范围

上一轮十二项真实运行有实际交付失败，也有初始响应误作最终响应、Gather 公开返回遗漏、原始未修改文件缺席、引用白名单过窄及缺证据被判半分等问题。不能把这些现象合称为产品退化。

复用 Case、Runner、双 View 双副本 Judge、逐项评分和离线报告。补全受控评测证据，采用新的评分和 Judge profile；拆分验收、比较和评价完整性。旧检查项继续按现有 goal/evidence/boundary 映射，权重 50/25/25 不变；协作仍是五细项及三组状态分布。本次不扩充任务，不修改产品上下文、Skill、普通用户存储或每日指标。

## 冻结方案 revision 1

- Suite 2.3.0 / scoring 2.1.0，任务题目、fixture、reference、verifier、关键条件和 480/600 秒预算保持。DEMO-106 的“审查后修复”要求不放宽。
- 使用现有十二项当前版本回归，一次 repetition、最多两个 Case 并行；Judge 每次阶段 600 秒；整套预算 7,200 秒，最多一个完整尝试。真实 Runtime 与真实 CLI Judge，模型及具体配置由执行前 plan.json 冻结。有效评价不重试以追求高分。
- 先运行合同和实现测试，再启动自动验收通道的独立 Core、数据、Skill Library、MCP 和工作区；不在用户原工作区重跑。
- 新证据包含预先声明的工作区文件及摘要校验的同 Turn 公开成员消息；Outcome 不读取 Process 轨迹。未知不当半分；单项引用无效只隔离该项，原输出保留。
- 通过标准仍是硬性验收、关键语义和专项条件均满足，必需评价无未知；已知失败不能被高分抵消。CLI 服务端 snapshot 无法固定的限制保留。每周单版本不能证明相对基线无退化。
- 这是已授权的评测器修正；不包含产品上下文改动。若再次改变评分含义或任务要求，需要独立版本和新的冻结计划，不能继续用本轮分数比较。

## 演示与验证

最终入口为真实运行目录的 report.html 和历史 index.html。按“结论 → Case → 双 Judge 理由 → 证据”查看，原始 JSON 与冻结计划保留。合成测试仅验证计算、证据隔离及展示，不能作为运行效果证据。

实施测试覆盖：同 View 合法引用；非法单项不污染其余项；旧 profile 保持严格行为；未修改文件与 Gather 返回的摘要绑定；跨 Turn、路径逃逸及篡改拒绝；每周失败不冒充退化；问题数与 Trial 数分开；HTML 转义和证据导航。

真实验收待运行。运行完成后追加具体路径、版本、样本数、结果、缺口及限制；不能用测试通过替代模型实测。

## 限制

未保留的私有命令细节不能追溯重建；相应声明可能仍未知。两个 Judge 副本可以有真实分歧，不强迫一致。一次十二项回归不能证明可靠性提升，也不替代独立验收集。旧证据链缺损或生产者版本不匹配时采用全新隔离运行，旧报告保留。

## 首次全套运行与补验范围（revision 2）

提交 596ad27a 于 2026-09-10 完成十二项真实运行，owner CLI configure / weekly 提交成功，实际约 28 分钟。原始目录为仓库外 `rovai-evaluation-evidence/complete-report-v3-20260910-6fzx31pg`。硬性通过 10/12；104 功能测试通过但产生两个越界空文件，106 未修改原始 stub，事务功能验收失败。三次 Trial 命中验收阻断（含 103 的关键交接部分满足），全部十二项均有评价缺口，总质量覆盖 72.57%，总分为空。没有基线，不能判定新退化。

104 的空正文进入旧非空文本 schema，106 新增的嵌套文件段 ID 含斜杠而不符合稳定 ID schema，导致二者 Judge 尚未调用。三个先失败的回归断言复现空正文、路径 ID 和模型可见文件名；修复只将空文件留在边界事实中，并将路径编码为合法 ID、在 View 中还原名称，不改变正文、rubric、适用项、任务或验收规则。

对原始 104 / 106 保留证据做完整 source pack / View schema 重建，已验证两个失败事实仍在、嵌套文件可见且无空文本。补验因此限定为二者各一次真实 Judge 补评、最多并行二项，沿用同一 Suite 2.3.0 / scoring 2.1.0 和 Judge 模型，每项 Judge 阶段最多 600 秒；不重新执行任务。新生产者和冻结补验计划单独留存，属于修复实现偏差后的定点验证，不替换原始 Trial，不重新计算十二项总分，也不作为新旧产品对照。定时触发的 2700 秒限制未变；这轮是 owner CLI 手动提交的 7200 秒计划，不证明定时触发已经验收。

首次结果也暴露语义局限：声明的命令执行缺少权威回执；103 的交接引用 Task 正文未进入 View，Judge 据此判部分满足，不能直接据此证明真实交接不足。这些问题保留原始理由和分歧，不事后改分。


## 本轮报告交付

已完成同证据真实补评：104 / 106 各四次 Judge 调用均返回，两个 View Suite 均为 disagreement。与首次 24 次合计 32 次真实调用，16 个预先不适用的 Process 副本没有调用模型。未重新执行任务、未覆盖初次结果、未重算整套总分。原始证据重建验证保留了 104 的边界失败、106 的功能失败和未改动源码。

综合报告位于上述独立证据根目录的 `report.html` / `report.md`，首次明细在 `reports/2026-09-07/attempt-01/report.html`，补评计划及结果在 `judge-supplement/`。`interpretation.json` 绑定原始报告和补评摘要；`report-validation.json` 记录三页共 150 个链接及锚点检查、无外部脚本和事件处理器、报告摘要一致。浏览器 URL 策略拒绝打开本地 HTML，视觉验收明确未完成，未采用替代浏览器或放宽沙箱。

进一步人工复核发现：104 较早的完整 Lead 交付消息已披露两个未跟踪空文件，但 Outcome 只拿到最后的简短回执，因此补评的限制未披露结论缺少完整上下文。这与确定的越界文件事实分开；后续应定义面向用户的完整交付消息集合并重新冻结评测方案，不能事后将本轮 Judge 判定改为通过。Task 正文、验证命令回执及 Judge 执行 rubric 的一致性仍需后续完善。

本轮交付的是可追溯的完整诊断报告，包含已知失败、评测器故障、真实补评和剩余限制；不能据此宣布产品质量提升、自动 Gate 可放行、定时触发已验收或独立保留集通过。

最终完整本地检查：172 个 Vitest 文件 / 1737 项通过；末组 Node 260 项通过，1 个既有平台测试跳过；typecheck 与带明确 base 的文档门禁通过。真实 Judge 32 次进程均正常返回且未超时；这不消除逐项分歧与未知。

## 完成性复跑与阶段责任诊断

2026-09-10 在提交 ebc8520e 上按用户要求重新执行 104 / 106，各一次，保留上述所有历史结果。模型仍为 gpt-5.6-sol / medium，任务、权限、Suite 2.3.0 / scoring 2.1.0、600 秒任务预算与 600 秒 Judge 预算均保持；使用两个独立临时 Core、Skill Library、MCP 和工作区。104 功能、回归、写入范围及专项规则通过，耗时 565.6 秒；106 仍只审查未修复，耗时 246.8 秒，未耗尽预算。两项共八次真实 Judge 调用正常返回，质量覆盖 81.25%，完整总分仍为空。

对 106 另做一次明确两阶段责任的诊断变体 v1.2.0：在原始题目之前说明 Lead 在 review-duo 报告之后继续实施修复、测试和交付。原题正文完整保留，fixture、reference、verifier、模型、权限、评分及预算逐项校验未变；产品 Core / CLI 二进制摘要也相同。该变体通过独立 Case 准入后实跑，478.6 秒内完成代码与测试修改，全部硬性检查及 A2A 规则通过；四次真实 Judge 调用正常返回且配对一致。命令执行声明仍缺权威回执，质量覆盖 87.5%，总分不发布。

这说明相同函数要求可以在既定预算内实际完成，支持优先检查审查结束后继续修复的责任衔接，而非降低函数复杂度。原版 A2A 返回的冻结上下文已包含完整原始用户需求，故不支持“整个原请求没有传入”的解释。一次诊断成功不能证明唯一原因、稳定改善或正式 Gate 放行；本轮未修改产品上下文、内置 Skill 或正式 Suite，诊断变体不替换原 106 成绩，不与其他轮成功拼成十二项通过。

独立证据目录为仓库外 `rovai-evaluation-evidence/completion-rerun-20260910-yc62eqhg`。`index.html` / `summary.md` 连接原版复跑报告与 `phase-clarity-diagnostic/` 报告；`summary.json` 绑定各份原始结果和计划摘要，`diagnosis.json` 与 `complexity-audit.md` 说明诊断依据、验收覆盖限制及变体边界。三页 32 个链接检查通过，十二次真实 Judge 调用无进程失败，临时 Runtime 目录已清理。浏览器视觉验收仍未完成，未绕过既有 URL 策略。原始十二项报告保持不变。


## 2026-09-10：完整评分证据方案 revision 3

本轮在用户要求再次运行完整评分评测的授权内实施。隔离 Runner 在丢弃原始 Runtime payload 前保留有界验证回执，补充 Task 描述和面向用户的 Lead 公开交付集合。证据边界与 rubric 见 [Semantic Judge Views v4](../../contracts/semantic-judge-views-v4.md)，套件与评分见 [Execution Evaluation v6](../../contracts/execution-evaluation-v6.md)。产品模型上下文、共享 Skill、权重、硬验收及日常数据不变。

106 采用上轮实际验证过的阶段说明，版本 1.2.0；不简化函数或删除失败样本。回归集 12 项、每项一次、最多两个 Case 并行，真实 Runtime 使用 gpt-5.6-sol / medium，Judge 为真实 CLI 的同模型反序双副本。Case 使用既有 480/600 秒预算，Judge 总阶段最多 600 秒，整轮预算 7200 秒；本轮不实施选择性重评或分歧裁决。最终总分仅在所有必需项有有效判定时发布。

验证范围：源数据摘要与跨 Trial 排除，成功/失败/缺失/截断回执，Task 仅 Process 可见，公开交付不包含定向交接，旧 profile 不改变，未知分母与硬门槛不变；完成本地检查后运行真实 12 项。真实结果尚待本轮执行，测试通过不代表任务效果改善。普通用户无新增字段或自动采集，仅评测目录增大。


### revision 3 首次执行与导出器修正

源码 28923122 的真实执行先通过 16 项合同条件。101 完成硬验收与语义评价；102 硬验收通过，但两副本均指出独立聚合脚本缺少回执。检查发现 `rovai` 关键词过滤会误排除隔离路径和测试包名中的 `rovai-…`，不符合“只排除协作内容”的既定方案。

于 2026-09-10T13:32:50Z 通过本轮 Owner CLI 中断 Campaign，保留 `complete-score-v4-20260910-p6t549jq` 全部结果、Judge 输出及已启动 Trial。修正为识别实际 Rovai 可执行命令，路径脱敏后再检查 Skill/成员标记；增加临时路径、包名和绝对 CLI 路径的回归测试。它是证据准入实现修复，权重、rubric、Case 和未知规则不变。

下一轮在新目录和独立数据目录重新冻结，固定相同模型与预算，完整执行 12 项。不会把本轮不同 Case 的成功拼成新一轮分数。最多追加这一轮完整运行；若仍有证据缺口，报告如实保留。
