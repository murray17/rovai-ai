---
document_type: implementation-plan
version: v1.58
status: in_progress
last_updated: 2026-09-10
---

# 回归预算校准与真实 Judge

用户明确要求提高时间上限、允许有限并行并接通 Judge。复用当前 Case、Runner、Host、双 View、评分和报告；不修改产品上下文或日常数据。

Suite 2.2.0 将十二项时间上限翻倍为 480／600 秒，旧 Case 目录保留；题目、原始材料、参考产物和 verifier 字节不变。新执行配置冻结最多两个 Case 并行，同一 Case 的基线／候选仍顺序执行。配置进入比较身份；两小时手动 campaign 用于完整校准，45 分钟定时上限保持。共享主机并行不代表严格资源隔离或速度收益。

当前没有 API Key。CLI Judge 复用已登录 Codex，使用 gpt-5.6-sol/medium、每次最多 240 秒、不自动重试。独立 CLI 会话只接收其 View 的材料；本地运输探测核验没有工具，原模型目录声明、定制工具配置、二进制和可能残留的用户指令均摘要绑定。只记录 CLI 支持的推理参数，不捏造温度、seed 或 Token 上限。模型目录声明不是不可变权重；该路径用于真实诊断，不能放行需要固定 snapshot 的 Gate，不能作为 Formal Judge。

真实小样本已暴露并保留：Semantic Review CLI 的 process 局部变量遮蔽入口、旧配置 schema 强制 API 参数、未向真实模型提供完整输出字段导致格式不合格。分别补入口回归、独立 1.1 schema、明确结构化输出。未将 pass/fail 或错误字段在事后转换成合法判定。旧证据复制上的发布又遇到生产者版本不一致，未覆盖原记录；完整验收使用当前生产者重新执行。

公开硬性检查与真实 Judge 分开。已保留样本的格式验证中，双副本均返回七个合法 Outcome 判定及闭包内引用；该样本不是全套通过。完整运行、结果、超时及剩余问题在完成后追加；合同和单测不计为任务质量样本。

演示按[操作指南](../../development/evaluation.md)准备 CLI Judge，使用新 Suite 冻结 execution 配置，再通过 owner CLI 提交 weekly。查看报告的实际模型、预算、版本、Case 明细和 Judge 引用；原旧预算失败不被替换。


## 首次完整校准：907b2847

2026-09-10 通过真实 owner `rovai app eval weekly` 运行 Suite 2.2.0、评分 2.0.0、每项一次、两个 Case 并行。Host 从提交到完成用时 27 分 41 秒；十六项合同通过，十二项实际任务全部执行，未发生时间预算耗尽或尾部跳过。底层产物验收十一通过、一失败；报告消费口径为十通过、一失败、一证据不足。此轮不是基线／候选对照，不据此推断速度收益或质量提升。

| Case | 执行秒数 | 本次观察 |
|---|---:|---|
| 101 | 135.3 | 产物通过，真实 Judge 完成 |
| 102 | 71.9 | 产物通过，真实 Judge 完成 |
| 103 | 373.8 | 产物通过，Task 投影的摘要错误阻止后续评价 |
| 104 | 479.5 | 产物通过，Judge 读取到初次交接而漏掉后续 Lead 交付 |
| 105 | 224.7 | 产物通过，同样受到最终回复取证错误影响；Gather 成员回复覆盖仍有限 |
| 106 | 334.0 | 正常收尾但只完成审查，源码仍为占位实现，功能验收失败 |
| 107 | 99.5 | 产物与记忆规则通过，完成声明的证据充分性存在 Judge 分歧 |
| 108 | 100.6 | 产物通过，Judge 有分歧 |
| 109 | 130.2 | 产物通过，一份 Judge 输出引用超出该评价项范围，拒绝接收 |
| 110 | 99.9 | 产物通过，真实 Judge 完成 |
| 111 | 71.6 | 产物及实际工具失败规则通过，真实 Judge 完成 |
| 112 | 79.5 | 产物通过；Judge 对缺少独立佐证的详细验证声明判为部分满足 |

共保留二十八次真实 Judge 请求结果；103 未进入 Judge。五个 Case 的双 View 评价状态 complete、五个 disagreement、一个 unavailable；complete 仅表示评价完成，112 的关键项仍未满足。整个质量评价覆盖率 75.69%，总分不发布，报告结论 degraded。104／105 的部分评价基于错误选取的最终回复，不能直接归因于 Agent；107／109／112 对具体工具、历史来源或无访问声明的独立佐证仍受 Outcome 证据隔离限制。不得用最终回复自身证明这些过程声明。

两个取证缺陷按实现偏差修复，Case 与评分不变：当前 Task 使用 taskId，旧投影读取 id 产生 undefined，写盘时丢失但参与了摘要；以落盘 JSON 计算修订摘要，并保留正确 Task 标识。最终回复按同一 CampTurn 的原 Lead 身份覆盖 A2A 返回后的 Run，不再仅选首次 root Run，也不以晚到成员回复冒充 Lead 最终交付。原坏链及原错误评价保持不变；独立补验结果另行记录，不能回填为这轮完整通过。

仍未完成：修复后完整新旧对照、独立保留集、不可变模型 snapshot、生产使用趋势。Gather 回复内容未全部进入当前 Process 证据包；有此缺口时不能用调用次数或 Lead 自述替代成员贡献证据。第一轮暴露的问题不是修改评分标准、减少协作要求或删除失败样本的依据。


## 定点补验与剩余索引修复

取证修复提交 2e681b2b 后，103／104 在新目录各补验一次，未改题目或评分。104 用时 387.4 秒，硬性及专项检查通过，最终回复确实来自原 Lead 的后续 Run；真实双副本 Judge 已执行，但仍有语义分歧，不能当作质量通过。103 产生两个真实 Task，暴露证据索引仍用旧 id 导致 `core.task:undefined` 重复；Runtime 没有超时，但评测器在结果发布前异常。该失败及原始执行仍保留。

继续修正 Evidence Index 的 Task／Task State 标识，以及 Runner 的 Task 工具结果关联；当前 taskId 优先，保留历史 id 兼容，缺少标识时明确拒绝。双 Task 回归先复现失败，再通过；真实保留 Task 字段的独立投影验证了两个可区分的索引。该投影验证不是完整 Trial 或质量样本。本轮最多再做一次 103 定点 Runtime 验证，不以重跑筛选高分。


最终索引修复提交 c4c45eb3 的 103 定点验证已完成：原 600 秒预算，实际 329.7 秒，产物及专项规则通过；真实 Task 标识、结果链和索引完整读取，四次 Judge 请求均返回。委派、贡献及 Lead 整合一致满足，交接充分性一致部分满足，完成声明仍有分歧；不认定语义 Gate 通过。隔离进程已退出，按本轮上限停止追加 Runtime 重跑。完整本地检查为 172 个 Vitest 文件／1736 项通过、末组 Node 256 通过及 1 个既有平台跳过；文档治理通过。原全套报告及两轮定点补验分别保留，不拼成全套成功。
