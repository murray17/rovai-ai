---
document_type: qualification-record
version: v1.71
authority: lark-real-tenant-qualification-evidence
status: in_progress
last_updated: 2026-09-24
---

# v1.71 Lark 真实租户验收记录

本记录用于执行 [Lark Channel v1 能力 Gate](../../contracts/lark-channel-v1.md#8-能力-gate)。自动化测试不能替代
真实 Lark 租户与客户端证据。任何未实际完成或证据不足的项目保持“未验证”，不得据此移除 Renderer 未验收提示，
也不得在发布说明或根 README 宣称支持 Lark。

## 证据安全规则

- 截图只保留能力结果与必要的脱敏标识；裁去二维码、Cookie、Token、Secret、CSRF、完整用户 ID、租户 ID、App ID
  和群 ID。必须区分对象时，只记录不可逆摘要的前 8 位。
- 日志只记录阶段、结果、错误码、时间和脱敏摘要；不得粘贴请求头、Cookie jar、credential payload、原始身份响应或
  完整 WebSocket 事件。
- 证据存放位置只写相对路径或受控记录 ID，不在本文放认证材料。失败证据与成功证据同样保留，不用后一次通过覆盖。

## 执行信息

| 字段 | 记录 |
| --- | --- |
| 执行日期 | 2026-09-24 |
| 执行人 | Principal（本机操作）；本记录由验真按 Camp 消息整理 |
| 构建来源与 Git commit | 分支 `rovai/lark-channel-v1.67` 工作区未提交代码，基线 `ec290dc6`；`pnpm dev` 开发实例，Core 以 Rust 1.90.0 编译。第 2、3 次尝试前分别修复了 Main 的 kind 准入与 Lark 用户摘要命名空间，未另行记录 commit |
| App 版本与平台 | 开发实例，`apps/desktop` 版本 0.3.2；macOS arm64 |
| Lark 客户端版本与平台 | 未记录 |
| 隔离 data root / userData 说明 | `pnpm dev` 自动隔离的开发 userData（`rovai-ai-development/<hash>/user-data`）；该实例全新初始化，没有飞书连接 |
| 租户与账号摘要 | 未记录（larksuite.com 国际版租户） |
| 证据根目录或记录 ID | Camp `rvcamp_01m36vfjv9e88rj6fx03b44k08` 消息 `27ffc06c`、`4ef17655`、`92b7dc33`、`3da94df1`；无截图入档 |

本轮只有终端阶段日志与 Principal 口述，没有截图。每项只按实际观察到的分支判定；预期中含未触发的分支时保持
“未验证”，已观察部分写在备注里。

### 尝试记录

| 次序 | Camp 消息 | 结果 | 处置 |
| --- | --- | --- | --- |
| 1 | `27ffc06c` | 失败：点击“登录开放平台”报 `Invalid channel kind`，请求在桌面 IPC 的 kind 准入被拒，未到达 Lark 登录 | Main 准入改为按 `ChannelKind` 穷举，补 `channel-kind-input.test.ts` |
| 2 | `4ef17655` | 失败：扫码与门户交接成功，停在“核对保存结果”；Main 以 `feishu-user` 计算 Lark 用户摘要，Core 在网关前拒绝，未留下回执 | 摘要改为按 Provider Profile 计算，补飞书/Lark 参数化测试 |
| 3 | `92b7dc33`、`3da94df1` | Principal 确认连接成功，并已在 Lark 客户端正常对话 | 无逐项日志与截图 |

状态只使用：`通过`、`失败`、`未验证`、`阻塞`。每项必须先填实际步骤，再判定结果。

## Q1 开发者账号扫码连接

| 检查项 | 实际步骤 | 预期 | 状态 | 脱敏证据与备注 |
| --- | --- | --- | --- | --- |
| 扫码初始化 | 在 Lark 页签点“登录开放平台”（第 2 次尝试） | 从 Lark 入口创建二维码会话，不访问飞书站点 | 通过 | `4ef17655`：`accounts.larksuite.com/accounts/qrlogin/init` 200；该次日志中的请求均为 larksuite 域 |
| 扫码轮询 | 手机 Lark 扫码并确认 | 待扫码、已扫码、已确认状态顺序可观察，超时与取消不提交账号 | 未验证 | 已观察：`awaiting_scan` → `scan_confirmed`，`qrlogin/polling` 均 200；未触发超时与取消 |
| 门户交接 | 扫码确认后自动交接 | 最终进入 `open.larksuite.com`；任一跳落入飞书站点时拒绝 | 未验证 | 已观察：`completing_login` 阶段 `open.larksuite.com/app` 200；落入飞书站点的拒绝分支未在真实租户触发，仅由自动化测试覆盖 |
| 身份读取 | 自动进入 `inspecting_identity` | 读取 Lark 身份并归一化为 `brand=lark`；站点声明不一致时拒绝 | 未验证 | 第 2 次尝试已越过身份读取、失败在本地提交；第 3 次连接成功间接表明身份读取成功，但无该阶段独立日志；不一致拒绝分支未触发 |
| 本地提交 | 第 2 次失败（见尝试记录）；修复后第 3 次重新扫码 | Lark 账号变为 connected，飞书账号与 Session 不变化 | 未验证 | Lark connected 仅有 Principal 口述（`3da94df1`），无截图；实例中没有飞书连接，飞书侧不变化无从观察，归 Q4 |
| 重启恢复 | 未执行 | 只恢复 Lark Developer Session；过期或身份变化时给出 Lark 专属失败码 | 未验证 | 第 2、3 次之间重启过开发实例，但未记录重启后的恢复状态 |

## Q2 队员 Bot 发布

| 检查项 | 实际步骤 | 预期 | 状态 | 脱敏证据与备注 |
| --- | --- | --- | --- | --- |
| 创建应用 | 未执行 | 只创建一个 Lark 应用，重试不重复创建远端身份 | 未验证 | 无 |
| 上传头像 | 未执行 | 应用与队员头像一致，失败可见且不越过后续 Gate | 未验证 | 无 |
| 启用能力 | 未执行 | Bot、权限、事件与长连接模式配置完成并回读核对 | 未验证 | 无 |
| 发布版本 | 未执行 | 版本创建并发布成功，远端状态可回读 | 未验证 | 无 |
| 凭据提交 | 未执行 | 凭据只进入 Main 安全存储；Renderer、Core 回执与日志无 Secret | 未验证 | 无 |
| WebSocket 启动 | 未逐项记录 | 已发布 Bot 使用 Lark 域连接；飞书 Host 状态不变化 | 未验证 | Principal 口述已在 Lark 客户端正常对话（`3da94df1`），表明至少一个 Bot 完成发布且收发消息；发布各阶段、Bot 数量、日志与截图均缺，Q2 其余行同理 |
| 发布重试 | 未执行 | 从失败阶段有界恢复，不复制应用或覆盖飞书 Bot | 未验证 | 无 |

## Q3 客户端消息与协作

| 检查项 | 实际步骤 | 预期 | 状态 | 脱敏证据与备注 |
| --- | --- | --- | --- | --- |
| 私聊与 `/new` | 未逐项记录 | Owner 私聊可运行；`/new` 轮换 Quick Chat；非 Owner 输入不触发 | 未验证 | Principal 口述“正常在 Lark 对话”，未说明是私聊还是群聊；`/new` 与非 Owner 输入未执行 |
| 群聊绑定 | 未执行 | 首次绑定卡完成项目或 Quick Chat 绑定；绑定后不可静默换绑 | 未验证 | 无 |
| 多 Bot 聚合 | 未执行 | 同一消息提及多个 Lark Bot 时生成一个 CampMessage 和有序 Run | 未验证 | 无 |
| 群 roster | 未执行 | Lark 群 Bot 与本机已发布目录交集正确，不混入飞书 Bot | 未验证 | 无 |
| 引用回复 | 未执行 | 被引用内容只进入本次 ExternalQuote，不扩大后续上下文 | 未验证 | 无 |
| 执行卡 | 未执行 | 最近输出、停止执行和 Owner 校验有效，卡片回调走 Lark 域 | 未验证 | 无 |
| 永久正文 | 未执行 | 最终正文按实际作者 Bot 投递，重试不重复产生永久结果 | 未验证 | 无 |
| 附件 | 未执行 | 显式附件交付成功；失败保持失败或未决，不伪装为正文成功 | 未验证 | 无 |

## Q4 隔离、失败与恢复

| 检查项 | 实际步骤 | 预期 | 状态 | 脱敏证据与备注 |
| --- | --- | --- | --- | --- |
| 飞书与 Lark 同时连接 | 未执行 | 两个账号、Bot、会话与 Snapshot 同时存在且独立 | 未验证 | 无 |
| Lark Host 失败 | 未执行 | 飞书与钉钉继续运行，Lark 显示独立失败 | 未验证 | 无 |
| 飞书 Host 失败 | 未执行 | Lark 与钉钉继续运行，飞书显示独立失败 | 未验证 | 无 |
| 对方站点拒绝 | 未执行 | Lark 流程拒绝飞书站点，飞书流程拒绝 Lark 站点，且无本地提交 | 未验证 | 无 |
| 会话过期与身份变化 | 未执行 | 失败码映射到 Lark 重连状态，不断开或覆盖飞书账号 | 未验证 | 无 |
| 应用重启 | 未执行 | 两个 Host 独立恢复，各自 Bot 只连接对应 SDK 域 | 未验证 | 无 |

## Gate 汇总

| 能力 | 结论 | 依据 |
| --- | --- | --- |
| Provider 身份、请求面、存储与隔离 | 未验证 | S2+S3 自动化验收通过，见[实施与验收](implementation-plan.md)；S4 Renderer 与 Web 尚未验收 |
| 开发者账号扫码连接 | 未验证 | 扫码初始化通过；轮询、交接的正向路径已观察；本地提交仅有口述，重启恢复与拒绝分支未执行 |
| 队员 Bot 发布 | 未验证 | 仅有端到端对话口述，无逐项证据 |
| 私聊、群聊、多 Bot、执行卡、永久输出、附件 | 未验证 | 仅有对话口述；群聊、`/new`、多 Bot、执行卡、附件未执行 |
| 失败隔离与恢复 | 未验证 | Q4 未执行 |

最终结论：**未验证**。只有所有必需项均为“通过”、失败记录有明确处置且完整门禁通过后，才能在后续版本提出解除
Lark 能力 Gate；本记录本身不修改合同状态。
