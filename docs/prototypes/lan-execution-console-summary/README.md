# 局域网执行台摘要行 HTML 设计稿

状态：本次设计评审稿，不是生产实现或产品合同。

直接打开 index.html 即可评审。稿件还原当前“设置 → 渠道”的字体、色彩、开放式分隔线和内容宽度，只重新设计“局域网执行台”摘要行的右侧状态与展开箭头。

本稿采用一个建议方案：

- 保留原生 details / summary，整行仍可由鼠标和键盘展开；
- 摘要固定为“说明 / 状态 / 28px 箭头槽位”三列；
- 使用居中的线性 SVG 替代受字体基线影响的字符箭头；
- 展开时只旋转 SVG，不移动槽位或状态文字；
- 日间、夜间和窄宽度使用同一结构。

页面中的账号、队员、地址和状态均为合成评审数据。当前生产语义仍由 docs/ui/components/channel-settings.md 与生产 Renderer 代码拥有。
